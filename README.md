  # Interactive Inventory Management System

  This is a code bundle for Interactive Inventory Management System. The original project is available at https://www.figma.com/design/tsuPi7U4EacJM6stsvNFRb/Interactive-Inventory-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  ---
  
  ## Changes Made
  - Setup TypeScript and Vite configurations properly.
  - Fixed various type errors in components (`Products.tsx`, `StockIn.tsx`, `StockOut.tsx`, `sidebar.tsx`, etc.).
  - Cleaned up unused imports and variables across pages.
  - Mocked and properly exported missing `buttonVariants` for Shadcn UI.